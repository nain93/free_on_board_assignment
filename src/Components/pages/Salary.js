import React from "react";
import styled from "styled-components";
import { GlobalContainer } from "../../GolbalStyles";

const Container = styled(GlobalContainer)``;

function Salary() {
  return <Container>Salary</Container>;
}

export default Salary;
